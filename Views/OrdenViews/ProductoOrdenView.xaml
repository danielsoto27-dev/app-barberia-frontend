<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:conv="clr-namespace:AppGestorVentas.Converters"
    xmlns:helpers="clr-namespace:AppGestorVentas.Helpers"
    x:Class="AppGestorVentas.Views.OrdenViews.ProductoOrdenView"
    xmlns:dtos="clr-namespace:AppGestorVentas.Models"
    Title="{Binding PageTitle}"
    x:Name="ProductoOrdenPage"
    Shell.TabBarIsVisible="False"
    Shell.FlyoutBehavior="Disabled">

    <!-- ============ RESOURCES ============ -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <conv:NullToBoolConverter x:Key="NullToBoolConverter" />
            <conv:BoolToStringConverter x:Key="BoolToStringConverter" />
            <conv:CountToBoolConverter x:Key="CountToBoolConverter" />

            <!-- 🔹 Headline label style -->
            <Style x:Key="HeadlineLabel" TargetType="Label">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="16" />
            </Style>

            <!-- 🔹 Border (card) style -->
            <Style x:Key="CardBorder" TargetType="Border">
                <Setter Property="Stroke" Value="LightGray" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="StrokeShape">
                    <Setter.Value>
                        <RoundRectangle CornerRadius="12" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Padding" Value="12" />
                <Setter Property="Margin" Value="0,6" />
            </Style>

            <!-- 🔹 Template: product / extra card -->
            <DataTemplate x:Key="ProductoCardTemplate" x:DataType="dtos:Producto">
                <Border Style="{StaticResource CardBorder}">
                    <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12">
                        <Image Grid.RowSpan="3"
                               WidthRequest="70"
                               HeightRequest="70"
                               Aspect="AspectFill"
                               Source="{Binding ImagenPrincipalUrl}" />

                        <Label Grid.Column="1"
                               Text="{Binding sNombre}"
                               FontAttributes="Bold" />

                        <Label Grid.Column="1" Grid.Row="1"
                               Text="{Binding iCostoPublico, StringFormat='Costo al público: ${0}'}"
                               FontSize="12" />

                        <Button Grid.Column="1" Grid.Row="2"
                                HorizontalOptions="End"
                                WidthRequest="110"
                                Text="{Binding isSeleccionado, Converter={StaticResource BoolToStringConverter}, ConverterParameter='Quitar|Seleccionar'}"
                                Command="{Binding Source={x:Reference ProductoOrdenPage}, Path=BindingContext.SeleccionarProductoCommand}"
                                CommandParameter="{Binding .}" />
                    </Grid>
                </Border>
            </DataTemplate>

            <!-- 🔹 Template: extra search result -->
            <DataTemplate x:Key="ExtraCardTemplate">
                <Border Style="{StaticResource CardBorder}">
                    <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12">
                        <Image Grid.RowSpan="3" WidthRequest="70" HeightRequest="70" Aspect="AspectFill" Source="{Binding aImagenes[0].sURLImagen}" />
                        <Label Grid.Column="1" Text="{Binding sNombre}" FontAttributes="Bold" />
                        <Label Grid.Column="1" Grid.Row="1" FontSize="12" Text="{Binding iCostoPublico, StringFormat='Costo al público: ${0}'}" />
                        <Button Grid.Column="1" Grid.Row="2" HorizontalOptions="End" WidthRequest="110" Text="Seleccionar" Command="{Binding Source={x:Reference ProductoOrdenPage}, Path=BindingContext.SeleccionarExtraCommand}" CommandParameter="{Binding .}" />
                    </Grid>
                </Border>
            </DataTemplate>

            <!-- 🔹 Template: selected extra list -->
            <DataTemplate x:Key="SelectedExtraTemplate">
                <Border Style="{StaticResource CardBorder}">
                    <Grid ColumnDefinitions="60,*,40">
                        <Image WidthRequest="50" HeightRequest="50" Aspect="AspectFill" Source="{Binding aImagenes[0].sURLImagen}" />
                        <Label Grid.Column="1" VerticalOptions="Center" Text="{Binding sNombre}" />
                        <Button Grid.Column="2" VerticalOptions="Center" FontFamily="MaterialDesignIcons" StyleClass="DeleteButton" Text="{x:Static helpers:MaterialDesignIcons.Delete}" Command="{Binding Source={x:Reference ProductoOrdenPage}, Path=BindingContext.EliminarExtraCommand}" CommandParameter="{Binding .}" />
                    </Grid>
                </Border>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>

    <!-- ============ UI ============ -->
        <Grid Padding="24,0">

            <!-- 🔸 1. Buscador de Productos -->
            <Grid RowDefinitions="50,*" IsVisible="{Binding BNoHayUnoProductoSeleccionado}">
               
                <Entry Grid.Row="0" Placeholder="Buscar platillo o bebida" Text="{Binding SBusquedaPrincipal}" />
                <CollectionView Grid.Row="1" 
                                ItemsSource="{Binding LstResultadosProductos}"
                                ItemTemplate="{StaticResource ProductoCardTemplate}" />
            </Grid>

            <!-- 🔸 2. Producto Seleccionado + Variante -->
            <Border IsVisible="{Binding OProductoSeleccionado, Converter={StaticResource NullToBoolConverter}}" 
                    Style="{StaticResource CardBorder}"
                    VerticalOptions="Start">
            
                <Grid RowDefinitions="Auto,Auto,70">
                    <!-- Card del producto -->
                    <Grid Grid.Row="0" ColumnDefinitions="80,*,35" RowDefinitions="Auto,40" ColumnSpacing="12" BindingContext="{Binding OProductoSeleccionado}">
                        <Image Grid.RowSpan="3" WidthRequest="70" HeightRequest="70" Aspect="AspectFill" Source="{Binding aImagenes[0].sURLImagen}" />
                        <Label Grid.Column="1" Text="{Binding sNombre}" FontAttributes="Bold" />
                        <Label Grid.Column="1" Grid.Row="1" FontSize="12" Text="{Binding iCostoPublico, StringFormat='Costo: ${0}'}" />
                        <Button Grid.Column="2" 
                                Grid.Row="0" 
                                Text="{x:Static helpers:MaterialDesignIcons.Delete}"
                                FontFamily="MaterialDesignIcons"
                                TextColor="Crimson"
                                Padding="0"
                                FontSize="30"
                                BackgroundColor="Transparent"
                                Command="{Binding Source={x:Reference ProductoOrdenPage}, Path=BindingContext.QuitarProductoCommand}" 
                                HorizontalOptions="End"
                                VerticalOptions="Start"
                                WidthRequest="30" />
                    </Grid>

                    <Grid Grid.Row="1" ColumnDefinitions="*,*" RowDefinitions="50,Auto">

                        <!-- Picker variante -->
                        <Label Grid.Column="0" Grid.Row="0" Text="Descripción" FontAttributes="Bold" VerticalTextAlignment="End" />

                        <Picker Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
                                    IsEnabled="False"
                                    TextColor="Black"
                                    ItemsSource="{Binding OProductoSeleccionado.aVariantes}"
                                    SelectedItem="{Binding VarianteSeleccionada}"
                                    ItemDisplayBinding="{Binding sVariante}" />

                        <!-- Controles de cantidad -->
                        <HorizontalStackLayout Grid.Column="1" Grid.Row="0" Spacing="2" HorizontalOptions="End" VerticalOptions="Start">
                            <Button Text="-" 
                                                WidthRequest="10" 
                                                HeightRequest="10"
                                                Padding="0"
                                                CornerRadius="37"
                                                FontSize="18"
                                                Command="{Binding DecrementarCantidadCommand}"/>

                        <Label Text="{Binding ICantidad}" 
                                               FontSize="16" 
                                               FontAttributes="Bold"
                                               VerticalOptions="Center"
                                               HorizontalOptions="Center"
                                               WidthRequest="30"
                                               HorizontalTextAlignment="Center"/>

                            <Button Text="+" 
                                                WidthRequest="10" 
                                                HeightRequest="10"
                                                Padding="0"
                                                CornerRadius="37"
                                                FontSize="18"
                                                TextColor="White"
                                                Command="{Binding IncrementarCantidadCommand}"/>
                        </HorizontalStackLayout>
                    </Grid>





                    <!-- 🔸 6. Confirmar -->
                    <Button Grid.Row="2" Text="{Binding ConfirmButtonText}"
                        Command="{Binding ConfirmarCommand}"
                        IsEnabled="{Binding OProductoSeleccionado, Converter={StaticResource NullToBoolConverter}}"
                        IsVisible="{Binding OProductoSeleccionado, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,24,0,0" />




                </Grid>
            </Border>
            
        </Grid>
</ContentPage>
